export default function Page() {
  return (
    <div>
      <h1>Routes</h1>
      <p>
        Home-to-home overlays work across distance (for example Sydney and Brisbane) when both agents are online.
        Kinlink provides encrypted routed access and named resources, not full layer-2 Wi-Fi emulation.
      </p>
      <ul>
        <li>Approve only required subnets or resources.</li>
        <li>Resolve overlaps (e.g. both homes using 192.168.1.0/24).</li>
        <li>Prefer per-resource publishing when overlap exists.</li>
      </ul>
    </div>
  );
}
